'use client';
import {useEffect,useState} from 'react';
import {platformApi} from '@/lib/platform';
export function GoogleSignIn(){
 const [available,setAvailable]=useState<boolean|null>(null),[busy,setBusy]=useState(false),[error,setError]=useState(false);
 useEffect(()=>{let alive=true;setError(new URLSearchParams(location.search).has('error'));platformApi('/capabilities').then(c=>{if(alive)setAvailable(c.authMode==='google-only'&&c.configured);}).catch(()=>{if(alive)setAvailable(false);});return()=>{alive=false;};},[]);
 return <section className="ops-card ops-auth"><h2>Continue with Google</h2><p>Use the same Google account to sign up or sign in. There are no OneBrain passwords. Your local canvas is not uploaded automatically.</p>{error&&<p role="alert">Google sign-in was not completed. It may have expired, been cancelled, or require operator configuration. Try again; legacy accounts need a reviewed identity migration.</p>}<form method="post" action="/api/auth/google/start" onSubmit={()=>setBusy(true)}><button className="ops-primary" disabled={!available||busy}>{busy?'Opening Google…':'Continue with Google'}</button></form>{available===null?<p role="status">Checking Google sign-in…</p>:!available?<p role="status">Google sign-in needs operator setup. Follow docs/GOOGLE-AUTH-SETUP.md. Device-local capture still works without an account.</p>:<p>Only basic identity is requested: name, email and Google account ID. Calendar, Gmail and Sheets permissions require separate explicit consent.</p>}<p><a href="/auth/login" target="_blank" rel="noopener noreferrer">Open sign-in in a separate tab</a> when using an embedded preview.</p><a href="/">Continue with device-local capture</a></section>;
}
